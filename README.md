# 🔒 سیستم دمو شاپرک - پلتفرم تحلیل داده امن

## ✨ ویژگی‌ها

✅ **احراز هویت امن** با JWT و زمان‌بندی خودکار  
✅ **محدودیت دسترسی دیتابیس** با ناشناس‌سازی خودکار  
✅ **محدودیت نصب پکیج** - فقط پکیج‌های مجاز  
✅ **غیرفعال‌سازی دانلود فایل**  
✅ **ثبت تمام کدهای اجرا شده** در دیتابیس  
✅ **پورتال مدیریت** با داشبورد و آمار  
✅ **نمایش زمان باقیمانده جلسه** به کاربر  

## 🚀 راه‌اندازی سریع

### پیش‌نیازها
- Docker & Docker Compose
- 8GB RAM
- 20GB فضای خالی

### نصب

```bash
# 1. Clone یا استخراج فایل‌ها
cd shaparak-jupyter-demo

# 2. راه‌اندازی
chmod +x setup.sh
./setup.sh

# 3. بررسی وضعیت
docker-compose ps

# 4. مشاهده لاگ‌ها
docker-compose logs -f
```

### دسترسی

- **پورتال**: http://localhost:3000
- **JupyterHub**: http://localhost:8000
- **API**: http://localhost:8001/docs

### کاربران دمو

| نام کاربری | رمز عبور | نقش |
|------------|---------|-----|
| admin | shaparak123 | مدیر |
| ali.rezaei | shaparak123 | کاربر عادی |
| sara.ahmadi | shaparak123 | کاربر عادی |

## 📊 دیتای دمو

- **1000 مشتری** با اطلاعات ناشناس
- **10,000 تراکنش** مالی
- آمار و گزارش‌های آماده

## 🧪 تست ویژگی‌ها

### 1. تست احراز هویت و تایمر
1. وارد پورتال شوید
2. زمان باقیمانده را مشاهده کنید
3. پس از 8 ساعت خودکار خارج می‌شوید

### 2. تست دسترسی دیتابیس
```python
# در Jupyter Notebook:
from shaparak_db_proxy import db

# دریافت مشتریان (ناشناس)
customers = db.get_customers()
print(customers.head())

# دریافت تراکنش‌ها
transactions = db.get_transactions(limit=100)
```

### 3. تست محدودیت نصب پکیج
```python
!pip install requests  # خطا: نصب مجاز نیست
```

### 4. تست ثبت لاگ
- هر کدی که اجرا کنید در دیتابیس ثبت می‌شود
- در پورتال می‌توانید لاگ‌ها را ببینید

## 🔧 توقف و حذف

```bash
# توقف
docker-compose stop

# حذف کامل
docker-compose down -v

# حذف images
docker rmi shaparak-jupyter-user:latest
```

## 📦 انتقال به سیستم دیگر

```bash
# 1. آرشیو کل پوشه
tar -czf shaparak-demo.tar.gz shaparak-jupyter-demo/

# 2. انتقال به سیستم جدید
scp shaparak-demo.tar.gz user@newserver:/path/

# 3. استخراج و اجرا
tar -xzf shaparak-demo.tar.gz
cd shaparak-jupyter-demo
./setup.sh
```

## 🐛 عیب‌یابی

```bash
# چک کردن لاگ‌ها
docker-compose logs jupyterhub
docker-compose logs postgres
docker-compose logs portal-backend

# Restart سرویس خاص
docker-compose restart jupyterhub

# دسترسی به دیتابیس
docker-compose exec postgres psql -U shaparak_admin -d shaparak
```

## 📝 لایسنس
Demo purposes only - Shaparak © 2024